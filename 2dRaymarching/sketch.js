var obstacles = [];

function setup() {
  createCanvas(600,600);
}

function draw() {
  background(51);
}

class obstacle {
  constructor(x, y, r) {
    this.x = x;
    this.y = y;
    this.r = r;
  }
  show() {
    for(var i = 0 ; i < obstacles.length ; i++ ) {
      
    }
  }
}